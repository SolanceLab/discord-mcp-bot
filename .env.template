# Discord Bot Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Owner User ID (the bot will track this user's presence and respond to their mentions)
# To get your Discord user ID: Enable Developer Mode in Discord settings,
# then right-click your name and select "Copy User ID"
OWNER_USER_ID=your_discord_user_id_here

# Optional: Custom path for downloaded Discord attachments
# DISCORD_ATTACHMENTS_DIR=/path/to/attachments

# --- Cloud Deployment (Optional) ---
# These are only needed if deploying the bot to a cloud server (Fly.io, etc.)

# HTTP API Authentication (required for cloud mode)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# BOT_API_SECRET=your_secret_here
# BOT_API_PORT=3000

# Cloud Proxy Mode — set this to forward all MCP tool calls to your cloud instance
# Remove this line to revert to direct (local) mode
# BOT_API_URL=https://your-app.fly.dev

# --- External Memory API (Optional) ---
# If you have a journal/memory API, configure it here for cross-session context
# MEMORY_API_URL=https://your-memory-api.example.com
# MEMORY_API_TOKEN=your_memory_api_token_here
